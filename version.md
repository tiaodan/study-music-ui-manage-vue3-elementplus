############# v0.1
-- 跑通一个产品(本地) v0.1 -> 原项目上传，备份
1. 把原项目，原封不动上传

############# v0.2
-- 跑通一个产品(本地) v0.2 -> 仅UI: 归整下载的音乐文件，还没和后端联调
先弄成界面+api请求 (MusicToolPage.vue )

目的：把下载的歌曲，根据 歌手-专辑，名字、歌词名字重新格式化，规整进数据库，并移动到HDD
前端实现UI+接口请求即可
需求拆分：
1. 名字重新格式化。
	- 实现：api指定 歌手-专辑路径，自动遍历文件(mp3/wav/layic)名字,重新格式化(多作者-歌名.文件类型)
2. 移动到HDD
	- 实现：api指定路径，from - to ,根据路径，移动；移动后，查询是否移动成功
3. 规整进数据库
	- 实现：api指定路径，path:xx, 自动遍历文件(mp3/wav/layic)名字,
		-- 插入作者。如果有新作者，插入数据库
		-- 把歌曲名，插入song表
		-- 关联song 和 singer关系

############# v0.3
-- 跑通一个产品(本地) v0.3 -> 仅UI: 音乐工具栏，可以将下载的歌曲，移动到HDD，并把歌曲信息规整进数据库! 和后端联调完成
先弄成界面+api请求 (MusicToolPage.vue )

目的：把下载的歌曲，根据 歌手-专辑，名字、歌词名字重新格式化，规整进数据库，并移动到HDD
前端实现UI+接口请求即可
需求拆分：
1. 名字重新格式化。
	- 实现：api指定 歌手-专辑路径，自动遍历文件(mp3/wav/layic)名字,重新格式化(多作者-歌名.文件类型)
2. 移动到HDD
	- 实现：api指定路径，from - to ,根据路径，移动；移动后，查询是否移动成功
3. 规整进数据库
	- 实现：api指定路径，path:xx, 自动遍历文件(mp3/wav/layic)名字,
		-- 插入作者。如果有新作者，插入数据库
		-- 把歌曲名，插入song表
		-- 关联song 和 singer关系

############# v0.3.1
优化：“移动到HDD”,"一键导入" toPath 必须加上 歌手/专辑

############# v0.3.2
新增：aws 文件夹，单文件、多文件，增删改查接口，没测试

############# v0.3.3
新增：前端登录接口实现，改为token方式







注意：
1. node_moudles打包成 zip文件，下载后解压才能用上传此文件，为了以后改动过程种，npm装的插件有问题了，可以回到最初版本还能运行
2. 参考项目：https://github.com/Yin-Hongwei/music-website  的 music-manage目录